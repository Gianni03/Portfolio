export default function WorkExperience() {
  return (
    <div>
      <h2>Work Experience</h2>
      {/* <ul>
        {experience.description.map((item, i) => (
          <li key={i}>{item}</li>
        ))}
      </ul> */}
    </div>
  );
}
